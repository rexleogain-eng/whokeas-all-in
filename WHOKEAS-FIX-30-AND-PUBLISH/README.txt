WHOKEAS ALL IN — Fix, 30% Margin and Publish

1. Stop npm run dev using Ctrl+C.
2. Double-click RUN-WHOKEAS-FIX-30-AND-PUBLISH.cmd.
3. Keep the window open.
4. The tool backs up the changed files, removes the Product Control hydration mismatch,
   locks all automated and manual pricing to approximately 30% gross margin after
   payment fees, removes minimum-profit overrides, reprices existing CJ products,
   publishes eligible drafts, runs the production build, commits, and pushes main.
5. Vercel starts the production deployment from the GitHub main branch.

The tool never copies or displays .env.local.
